apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: wordpress
spec:
  interval: 5m
  chart:
    spec:
      chart: wordpress
      version: "23.1.0"
      sourceRef:
        kind: HelmRepository
        name: wordpress
      interval: 1m